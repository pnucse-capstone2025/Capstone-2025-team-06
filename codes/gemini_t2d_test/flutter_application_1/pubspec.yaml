name: flutter_application_1
description: "T2D Diet & Physical Activity coach (Gemini-powered)"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  # Gemini (Google Generative AI) SDK for Dart/Flutter
  google_generative_ai: ^0.4.6
  # Simple HTTP (optional but handy)
  http: ^1.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
